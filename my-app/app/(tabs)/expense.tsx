import { View, Text } from "react-native";

const expense = () => {
  return (
    <View className="flex-1 items-center justify-center p-20">
      <Text className="text-4xl">Expense</Text>
    </View>
  );
};

export default expense;
